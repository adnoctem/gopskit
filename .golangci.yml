version: "2"

run:
  timeout: 5m

linters:
  default: standard
  enable:
    - unused
    - unconvert
    - misspell
    - gocritic

formatters:
  enable:
    - gofmt
    - goimports

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
